extends layout.pug
include search-mixins.pug

block title
  title ingoodcompany.info - Research a company for your job search

block analytics
  script.
    mixpanel.track('Visited root');
    mixpanel.track_links('.search-results', 'Clicked default search results link');

block content
  +pageHeading({includeDescription: true})
  +searchForm(null, {autofocus: 'true'})
  .row
    .col-lg-6
      h4 Example results
  +searchResults({ query: defaultQuery, glassdoorInfo })
